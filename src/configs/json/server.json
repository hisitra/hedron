{
  "rpcPort": {
    "doc": "gRPC Port",
    "default": "3001",
    "env": "PORT",
    "format": "string"
  },
  "restPort": {
    "doc": "REST Port",
    "default": "4001",
    "env": "REST_PORT",
    "format": "string"
  },
  "requestTimeout": {
    "doc": "Request Timeout",
    "default": "5",
    "env": "HEDRON_REQUEST_TIMEOUT",
    "format": "int"
  }
}